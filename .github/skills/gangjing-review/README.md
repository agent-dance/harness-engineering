# gangjing-review

`gangjing-review` 是 `gangjing` 的 searchable alias，专门给 `skills.sh`
这类 registry / search 入口使用。

它保留相同的核心能力：

- 对方案和决策做杠精式质询
- 对当前工作区代码生成攻击配置
- 在明确授权下运行临时 harness 做代码实锤

canonical repo:

- `skills/gangjing/`
- `tooling/gangjing-engine/`
