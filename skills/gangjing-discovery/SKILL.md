---
name: gangjing-discovery
homepage: https://github.com/agent-dance/harness-engineering/tree/main/skills/gangjing
description: >
  Search and discovery entry for gangjing, the contrarian review and red-team
  skill. Use this entry to find the project on skills.sh, then install the full
  `gangjing` skill from the same repository when you need the complete attack
  workflow.
---

# Gangjing Discovery

这是 `gangjing` 的搜索入口。

用途：

- 让 `skills.sh` 的全局搜索更容易搜到 `gangjing`
- 给用户一个低风险入口，先理解这个技能在做什么
- 再把用户导向同仓库里的完整技能

主技能位置：

- `skills/gangjing/`

如果你需要完整能力：

1. 先阅读 `skills/gangjing/README.md`
2. 再直接从同仓库安装主技能 `gangjing`
3. 若需要代码攻击引擎，使用主技能里的 canonical engine / template workflow

这个 discovery entry 本身不捆绑可执行 harness，也不直接运行代码。
